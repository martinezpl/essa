export const psqlForeignKeySourceHandleId = (foreignKeyId: string) =>
  `psql-fk-source-${foreignKeyId}`;

export const psqlColumnTargetHandleId = (columnId: string) =>
  `psql-column-target-${columnId}`;
